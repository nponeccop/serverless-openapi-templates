service: validation-middy
frameworkVersion: '2'
provider:
  name: aws
  runtime: nodejs14.x
  lambdaHashingVersion: 20201221
plugins:
  - serverless-offline
functions:
  hello:
    handler: validatorMiddy.handler
    events:
      - httpApi:
          method: POST
          path: /validatorMiddy
